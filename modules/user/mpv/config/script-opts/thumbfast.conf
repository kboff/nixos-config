# Socket path (leave empty for auto)
socket=

# Thumbnail path (leave empty for auto)
thumbnail=

# 缩略图生成的最大尺寸（像素）（按比例缩小以适应）
# 启用高分辨率（hidpi）时，值会按比例调整
max_height=200
max_width=200

# Scale factor for thumbnail display size (requires mpv 0.38+)
# Note that this is lower quality than increasing max_height and max_width
scale_factor=1

# Apply tone-mapping, no to disable
tone_mapping=auto

# Overlay id
overlay_id=42

# 在文件加载时生成缩略图生成器，以加快初始缩略图的生成速度
spawn_first=no

# 在经过指定秒数的无活动期后关闭缩略图生成进程，0表示禁用
quit_after_inactivity=0

# Enable on network playback
network=yes

# Enable on audio playback
audio=no

# Enable hardware decoding
hwdec=yes

# Windows only: use native Windows API to write to pipe (requires LuaJIT)
direct_io=yes

# Custom path to the mpv executable
mpv_path=D:\mpv\mpv.exe

network=yes
